type: query
expected: SELECT count(l.id_publicacion) as likes_totales from likes l, publicaciones p, usuarios u where l.id_publicacion = p.id_publicacion and p.id_duenio = u.id_usuario and nombre_usuario = "valen_s" and fecha = date("now") AND NOT es_temporal;